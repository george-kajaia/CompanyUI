export const environment = {
  // Production API base URL — baked into `ng build` (the default `production`
  // configuration). Local development uses environment.development.ts instead,
  // swapped in automatically by angular.json fileReplacements for `ng serve`.
  // Do NOT point this at localhost: a production build must never ship the dev URL.
  apiBaseUrl: 'https://service-tokens.com/api'
};
