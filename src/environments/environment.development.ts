export const environment = {
  // Local development API base URL. Used by `ng serve` (the default `development`
  // configuration) via the fileReplacements entry in angular.json — no manual
  // editing of environment.ts required.
  apiBaseUrl: 'https://localhost:7044/api'
};
